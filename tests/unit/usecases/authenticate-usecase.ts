describe('AuthenticateUsecase', () => {
  it('should throw UnauthorizedError if user is found but password is wrong')

  it('should throw UnauthorizedError if user is not found')

  it('should throw UnauthorizedError if password is wrong')

  it('should return valid token if user is found and password is correct')
})
